{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "ci:bootstrap":"lerna bootstrap",
    "ci:build":"lerna run build --since $TAG",
    "ci:version":"lerna version --conventional-commits --yes"
  }
}
