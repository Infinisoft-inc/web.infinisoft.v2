{
  "name": "root",
  "private": true,
  "version": "independent",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "ci:build":"lerna run ci:build --since $TAG",
    "ci:version":"lerna version --conventional-commits --yes"
  }
}
